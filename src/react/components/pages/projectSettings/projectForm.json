{
    "type": "object",
    "properties": {
        "name": {
            "title": "${strings.common.displayName}",
            "type": "string",
            "pattern": "^[^\\\\\\\\/:*?\\\\\\\"<>|]*$"
        },
        "securityToken": {
            "title": "${strings.projectSettings.securityToken.title}",
            "description": "${strings.projectSettings.securityToken.description}",
            "type": "string"
        },
        "sourceConnection": {
            "title": "${strings.projectSettings.sourceConnection.title}",
            "description": "${strings.projectSettings.sourceConnection.description}",
            "type": "object"
        },
        "folderPath": {
            "title": "Folder path",
            "description": "Subfolder path to your files",
            "type": "string"
        },
        "formRecognizerBackend": {
            "title": "Form recognizer backend",
            "description": "Form recognizer backend",
            "type": "string",
            "format": "uri"
        },
        "ocrUriBase": {
            "title": "OCR service URI",
            "description": "OCR service URI",
            "type": "string",
            "format": "uri"
        },
        "description": {
            "title": "${strings.common.description}",
            "type": "string"
        }
    },
    "required": [
        "name",
        "sourceConnection",
        "formRecognizerBackend",
        "ocrUriBase"
    ]
}